version: '3.8'

services:
  iptv:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - iptv-data:/app/data
    environment:
      - IPTV_PORT=5000
      - IPTV_DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  iptv-data:
